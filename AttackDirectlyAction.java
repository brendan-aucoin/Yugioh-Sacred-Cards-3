package actions;

import cards.Monster;
import game.Game;
import player.Duelist;
import states.AttackingState;
import states.DuelingState;
import states.StateList;

public class AttackDirectlyAction extends Action{
	/*@Override
	/*public void performAction(Duelist player,Duelist opponent,Monster playersMonster,Game game) {
		useCard(playersMonster);
		if(DuelingState.firstTurn) {return;}//you cannot attack on the first turn
		//set the state of the game
		setNextState(game, player, opponent, playersMonster, null);
	}*/
}
